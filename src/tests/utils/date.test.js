import { render, fireEvent } from "@testing-library/react";
import getDate from "../../utils/date";

describe("Date Util tests", () => {
  test("getDate returns successfully", () => {});
  test("getDate returns error message on error", () => {});
});
